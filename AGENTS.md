# Project Delivery Workflow

- After every completed modification in this repository, synchronize the project files to `C:\Users\25949\Desktop\豆绘-拼豆图纸`.
- Treat `C:\Users\25949\Documents\New project` as the Git working tree. Create a Git commit for all completed project changes and push it to `origin/main`.
- Exclude Git metadata such as `.git` from the desktop synchronization. Do not overwrite unrelated, uncommitted work if either location contains changes that are not part of the current task.
- Skip synchronization, commit, or push only when the user explicitly asks to do so, or when a blocking error is reported clearly.
